(function(){
'use strict';
const INITIAL=10000;
const STATE_KEY='comparador_fondo3_last_query_v5';
const DRIVE_KEY='fondo3_drive_sync_key_v1';
const PRO_URL_KEY='profuturo_fondo3_drive_sync_url_v3';
const HAB_URL_KEY='habitat_fondo3_drive_sync_url_v3';
const PRO_DEFAULT='https://script.google.com/macros/s/AKfycbxY9JqIeTnweKaEXAOs7hQ6KftlPgVsGOFPwOp7hqL5gJ47OuuHlAJBksTGdOQ2yc_Y0Q/exec';
const HAB_DEFAULT='https://script.google.com/macros/s/AKfycbxoYHkCu0cZPx_KsMlI0Jd5PEATgxBjZTR8oK8qs1cUjRHJbiK0t-bxkH5ACprgp81S7g/exec';
let DATA=null;
const $=id=>document.getElementById(id);
const finite=x=>x!==null&&x!==undefined&&x!==''&&Number.isFinite(Number(x));
const money=x=>finite(x)?new Intl.NumberFormat('es-PE',{style:'currency',currency:'PEN',minimumFractionDigits:2,maximumFractionDigits:2}).format(Number(x)):'—';
const signedMoney=x=>finite(x)?`${Number(x)>=0?'+':'-'}${money(Math.abs(Number(x)))}`:'—';
const pct=x=>finite(x)?`${Number(x)>=0?'+':''}${(Number(x)*100).toFixed(2)}%`:'—';
const pctAbs=x=>finite(x)?`${(Number(x)*100).toFixed(2)}%`:'—';
const vc=x=>finite(x)?Number(x).toFixed(7):'—';
const fmt=s=>{if(!s)return'—';const p=String(s).slice(0,10).split('-');return p.length===3?`${p[2]}/${p[1]}/${p[0]}`:String(s)};
function readState(){try{return JSON.parse(localStorage.getItem(STATE_KEY)||'{}')||{}}catch(_e){return{}}}
function saveState(extra={}){const s=readState();const active=document.querySelector('#operationPanel [data-opmode].active');const out={...s,...extra,source:$('opSource')?.value||s.source||'sbs',mode:active?.dataset.opmode||s.mode||'inside',entry:$('opEntry')?.value||s.entry||'',exit:$('opExit')?.value||s.exit||'',capital:$('opCapital')?.value||s.capital||'10000'};try{localStorage.setItem(STATE_KEY,JSON.stringify(out))}catch(_e){}}
function officialMap(rows){const m=new Map();for(const r of(Array.isArray(rows)?rows:[])){const d=String(r?.fecha||'').slice(0,10),v=Number(r?.vc);if(d&&Number.isFinite(v)&&v>0&&(r?.es_oficial===true||String(r?.fuente||'').toUpperCase().includes('SBS')))m.set(d,v)}return m}
function dualActualMap(db){const out=new Map();for(const key of['qqq','new_tickers']){const m=db?.models?.[key]||{};for(const arr of[m.history_one_step||[],m.history_operational||[],m.forward_chain||[]])for(const r of arr){const d=String(r?.fecha||'').slice(0,10),v=Number(r?.actual_vc);if(d&&Number.isFinite(v)&&v>0)out.set(d,v)}const c=m.current,d=String(c?.fecha||'').slice(0,10),v=Number(c?.actual_vc);if(d&&Number.isFinite(v)&&v>0)out.set(d,v)}const ld=String(db?.latest_sbs?.fecha||'').slice(0,10),lv=Number(db?.latest_sbs?.vc);if(ld&&Number.isFinite(lv)&&lv>0)out.set(ld,lv);return out}
function officialHybrid(series,dual){const out=officialMap(series);for(const[d,v]of dualActualMap(dual))out.set(d,v);return out}
function modelMap(db,key){const out=new Map(),m=db?.models?.[key]||{};const add=(r,overwrite=false)=>{const d=String(r?.fecha||'').slice(0,10),v=Number(r?.vc_estimated);if(d&&Number.isFinite(v)&&v>0&&(overwrite||!out.has(d)))out.set(d,v)};for(const r of(m.history_one_step||[]))add(r);for(const r of(m.history_operational||[]))add(r);for(const r of(m.forward_chain||[]))add(r);if(m.current)add(m.current,true);return out}
function pair(pm,hm){return[...pm.keys()].filter(d=>hm.has(d)).sort().map(fecha=>({fecha,profuturo:pm.get(fecha),habitat:hm.get(fecha)}))}
function volatility(values){if(values.length<3)return null;const rets=[];for(let i=1;i<values.length;i++)rets.push(values[i]/values[i-1]-1);if(rets.length<2)return null;const mean=rets.reduce((a,b)=>a+b,0)/rets.length;const variance=rets.reduce((a,b)=>a+(b-mean)**2,0)/(rets.length-1);return Math.sqrt(variance)*Math.sqrt(252)}
function selectMain(period){const all=DATA?.sbs||[];if(period==='all')return all.slice();if(period==='2026')return all.filter(r=>r.fecha>='2026-01-01');const n=Number(period||30);return all.slice(Math.max(0,all.length-n))}
function drawMain(period){const rows=selectMain(period);if(rows.length<2)return;const first=rows[0],last=rows.at(-1);const hv=rows.map(r=>r.habitat),pv=rows.map(r=>r.profuturo),h=hv.map(v=>INITIAL*v/first.habitat),p=pv.map(v=>INITIAL*v/first.profuturo);const hr=h.at(-1)/INITIAL-1,pr=p.at(-1)/INITIAL-1,vh=volatility(hv),vp=volatility(pv);$('periodText').textContent=`${fmt(first.fecha)} → ${fmt(last.fecha)} · ${rows.length} fechas SBS comunes`;$('retHab').textContent=pct(hr);$('retPro').textContent=pct(pr);$('capHab').textContent=`Capital final: ${money(h.at(-1))}`;$('capPro').textContent=`Capital final: ${money(p.at(-1))}`;$('volHab').textContent=vh===null?'—':pctAbs(vh);$('volPro').textContent=vp===null?'—':pctAbs(vp);$('retHab').classList.toggle('better',hr>pr);$('retPro').classList.toggle('better',pr>hr);$('volHab').classList.toggle('better',vh!==null&&vp!==null&&vh<vp);$('volPro').classList.toggle('better',vh!==null&&vp!==null&&vp<vh);$('summary').textContent=`Mayor rentabilidad: ${hr>pr?'Hábitat':pr>hr?'Profuturo':'Empate'} · Menor volatilidad: ${vh!==null&&vp!==null?(vh<vp?'Hábitat':vp<vh?'Profuturo':'Empate'):'—'}`;Plotly.react('chart',[{x:rows.map(r=>r.fecha),y:h,type:'scatter',mode:'lines',name:`Hábitat · ${money(h.at(-1))}`,line:{width:3},hovertemplate:'<b>Hábitat</b><br>%{x}<br>Capital: S/%{y:,.2f}<extra></extra>'},{x:rows.map(r=>r.fecha),y:p,type:'scatter',mode:'lines',name:`Profuturo · ${money(p.at(-1))}`,line:{width:3},hovertemplate:'<b>Profuturo</b><br>%{x}<br>Capital: S/%{y:,.2f}<extra></extra>'}],{paper_bgcolor:'#0f1b2d',plot_bgcolor:'#0b1728',font:{color:'#e2e8f0',size:11},margin:{l:62,r:18,t:35,b:50},hovermode:'x unified',legend:{orientation:'h',y:1.12,x:0},xaxis:{gridcolor:'#243244',title:'Fecha SBS'},yaxis:{gridcolor:'#243244',title:'Valor de la inversión (S/)',tickprefix:'S/ '},showlegend:true},{responsive:true,displaylogo:false,displayModeBar:false});saveState({period})}
function installMainControls(){const box=$('controls');if(!box)return;box.innerHTML='';const saved=readState().period||'30';for(const[k,label]of[['30','30 sesiones'],['60','60 sesiones'],['90','90 sesiones'],['180','180 sesiones'],['2026','2026'],['all','Todo']]){const b=document.createElement('button');b.type='button';b.textContent=label;b.classList.toggle('active',k===saved);b.onclick=()=>{[...box.children].forEach(x=>x.classList.toggle('active',x===b));drawMain(k)};box.appendChild(b)}drawMain(saved)}
function sourceLabel(s){return s==='a'?'Modelo A · Rolling 30':s==='b'?'Modelo B · Rolling 30':'SBS real'}
function opRows(){return DATA?.sets?.[$('opSource')?.value||'sbs']||[]}
function mode(){return document.querySelector('#operationPanel [data-opmode].active')?.dataset.opmode||'inside'}
function effectiveEntry(req,rs){return rs.find(r=>r.fecha>=req)||null}
function effectiveExit(req,rs){for(let i=rs.length-1;i>=0;i--)if(rs[i].fecha<=req)return rs[i];return null}
function paint(id,val,isPct=false){const e=$(id);if(!e)return;e.textContent=isPct?pct(val):signedMoney(val);e.classList.toggle('pos',Number(val)>0);e.classList.toggle('neg',Number(val)<0)}
function calculate(){const rs=opRows(),src=$('opSource')?.value||'sbs';if(rs.length<2){$('opWinner').textContent=`${sourceLabel(src)}: no hay fechas comunes suficientes.`;return}const req=$('opEntry')?.value,cap=Number($('opCapital')?.value||0);if(!req||!Number.isFinite(cap)||cap<=0){$('opWinner').textContent='Completa fecha de entrada y monto.';return}const entry=effectiveEntry(req,rs);if(!entry){$('opWinner').textContent='No existe una fecha efectiva de entrada para esa fuente.';return}let exit,requestedExit='';if(mode()==='closed'){requestedExit=$('opExit')?.value;if(!requestedExit){$('opWinner').textContent='Indica la fecha de salida.';return}exit=effectiveExit(requestedExit,rs)}else exit=rs.at(-1);if(!exit||exit.fecha<entry.fecha){$('opWinner').textContent='La fecha efectiva de salida debe ser posterior o igual a la entrada.';return}const hf=cap*Number(exit.habitat)/Number(entry.habitat),pf=cap*Number(exit.profuturo)/Number(entry.profuturo),hg=hf-cap,pg=pf-cap,hr=hf/cap-1,pr=pf/cap-1;$('opHabFinal').textContent=money(hf);$('opProFinal').textContent=money(pf);paint('opHabGain',hg);paint('opProGain',pg);paint('opHabRet',hr,true);paint('opProRet',pr,true);$('opHabMeta').textContent=`${sourceLabel(src)} · ${fmt(entry.fecha)} VC ${vc(entry.habitat)} → ${fmt(exit.fecha)} VC ${vc(exit.habitat)} · escala propia Hábitat`;$('opProMeta').textContent=`${sourceLabel(src)} · ${fmt(entry.fecha)} VC ${vc(entry.profuturo)} → ${fmt(exit.fecha)} VC ${vc(exit.profuturo)} · escala propia Profuturo`;const winner=hf>pf?'Hábitat':pf>hf?'Profuturo':'Empate',diff=Math.abs(hf-pf),adjust=[];if(entry.fecha!==req)adjust.push(`entrada ajustada ${fmt(req)}→${fmt(entry.fecha)}`);if(requestedExit&&exit.fecha!==requestedExit)adjust.push(`salida ajustada ${fmt(requestedExit)}→${fmt(exit.fecha)}`);$('opHabCard').classList.toggle('winner',hf>pf);$('opProCard').classList.toggle('winner',pf>hf);$('opWinner').textContent=winner==='Empate'?`${sourceLabel(src)} · empate · ${fmt(entry.fecha)} → ${fmt(exit.fecha)} · ${money(hf)}.`:`${sourceLabel(src)} · más rentable: ${winner} · ventaja ${money(diff)} · ${fmt(entry.fecha)} → ${fmt(exit.fecha)}${adjust.length?' · '+adjust.join(' · '):''}.`;saveState()}
function installOperation(){const p=$('operationPanel');if(!p)return;p.innerHTML=`<div class="head"><div><h2>Operación comparada · Hábitat vs Profuturo</h2><div class="sub">Mismo monto y mismas fechas. El VC nominal de cada AFP tiene una escala propia; se compara la variación porcentual, no si 69 es mayor que 33.</div></div></div><div class="opmodes"><button type="button" class="opbtn active" data-opmode="inside">Sigo dentro</button><button type="button" class="opbtn" data-opmode="closed">Ya salí</button></div><div class="opinputs"><label>Fuente de valoración<select id="opSource"><option value="sbs">SBS real</option><option value="a">Modelo A · Rolling 30</option><option value="b">Modelo B · Rolling 30</option></select></label><label>Fecha de entrada<input type="date" id="opEntry"></label><label>Capital invertido (S/)<input type="number" id="opCapital" value="10000" min="1" step="100"></label><label id="opExitWrap" class="ophidden">Fecha de salida<input type="date" id="opExit"></label><button class="opbtn primary" id="opCalc" type="button">Calcular operación</button></div><div class="opwinner" id="opWinner">Cargando datos…</div><div class="opresults"><div class="opfund" id="opHabCard"><h3>Hábitat Fondo 3</h3><div class="opmetrics"><div class="opmini"><span>Capital final</span><b id="opHabFinal">—</b></div><div class="opmini"><span>Ganancia / pérdida</span><b id="opHabGain">—</b></div><div class="opmini"><span>Rentabilidad</span><b id="opHabRet">—</b></div></div><div class="opmeta" id="opHabMeta">—</div></div><div class="opfund" id="opProCard"><h3>Profuturo Fondo 3</h3><div class="opmetrics"><div class="opmini"><span>Capital final</span><b id="opProFinal">—</b></div><div class="opmini"><span>Ganancia / pérdida</span><b id="opProGain">—</b></div><div class="opmini"><span>Rentabilidad</span><b id="opProRet">—</b></div></div><div class="opmeta" id="opProMeta">—</div></div></div><div class="note" style="margin-top:10px"><b>Importante:</b> los VC 69 de Profuturo y 33 de Hábitat no se comparan entre sí porque cada AFP tiene su propia escala de cuota. Para la rentabilidad se usa VC salida / VC entrada dentro de cada AFP.</div>`;const s=readState();$('opSource').value=s.source||'sbs';const rs=opRows();if(rs.length){$('opEntry').value=s.entry||rs[Math.max(0,rs.length-30)].fecha;$('opExit').value=s.exit||rs.at(-1).fecha}$('opCapital').value=s.capital||'10000';document.querySelectorAll('#operationPanel [data-opmode]').forEach(b=>{b.classList.toggle('active',b.dataset.opmode===(s.mode||'inside'));b.onclick=()=>{document.querySelectorAll('#operationPanel [data-opmode]').forEach(x=>x.classList.toggle('active',x===b));$('opExitWrap').classList.toggle('ophidden',b.dataset.opmode!=='closed');calculate()}});$('opExitWrap').classList.toggle('ophidden',(s.mode||'inside')!=='closed');$('opCalc').onclick=calculate;['opEntry','opCapital','opExit'].forEach(id=>$(id).addEventListener('change',calculate));$('opSource').addEventListener('change',calculate);calculate()}
function officialCompare(entryDate,exitDate,capital,isOpen){const rs=DATA?.sbs||[];if(!rs.length||!entryDate||!finite(capital)||Number(capital)<=0)return null;const entry=effectiveEntry(String(entryDate).slice(0,10),rs);if(!entry)return null;let exit;if(isOpen)exit=rs.at(-1);else exit=effectiveExit(String(exitDate||'').slice(0,10),rs);if(!exit||exit.fecha<entry.fecha)return null;const cap=Number(capital),pf=cap*exit.profuturo/entry.profuturo,hf=cap*exit.habitat/entry.habitat;return{entry,exit,cap,pf,hf,pg:pf-cap,hg:hf-cap,pr:pf/cap-1,hr:hf/cap-1,diff:pf-hf}}
function jsonp(url,fund,key){return new Promise((resolve,reject)=>{if(!key)return reject(new Error('Falta la clave de Drive'));const cb='__cmpv5_'+Date.now()+'_'+Math.random().toString(16).slice(2),s=document.createElement('script');let done=false;const finish=(e,d)=>{if(done)return;done=true;clearTimeout(t);try{delete window[cb]}catch(_e){}s.remove();e?reject(e):resolve(d)};const t=setTimeout(()=>finish(new Error('Apps Script no respondió')),15000);window[cb]=d=>d?.ok===true?finish(null,d):finish(new Error(d?.error||'Respuesta inválida'));const u=new URL(url);u.searchParams.set('action','list');u.searchParams.set('fund',fund);u.searchParams.set('key',key);u.searchParams.set('callback',cb);u.searchParams.set('_',Date.now());s.onerror=()=>finish(new Error('No se pudo contactar Drive'));s.src=u.toString();document.head.appendChild(s)})}
function installDriveHistory(){if($('driveHistoryPanel'))return;const st=document.createElement('style');st.textContent=`.drivehist{margin-top:13px;background:var(--card);border:1px solid var(--line);border-radius:14px;padding:12px}.drivehistbar{display:flex;justify-content:space-between;gap:8px;align-items:end;flex-wrap:wrap}.drivehistconnect{display:flex;gap:6px;align-items:end;flex-wrap:wrap}.drivehistconnect input{background:#07111f;color:#fff;border:1px solid #35465d;border-radius:8px;padding:8px;min-width:220px}.drivehistconnect button{border:1px solid #1d4ed8;background:#2563eb;color:#fff;border-radius:8px;padding:8px 10px;font-weight:800}.drivehistlist{display:grid;gap:8px;margin-top:10px}.drivehistcard{background:#091728;border:1px solid #2b4059;border-radius:10px;padding:10px}.drivehisthead{font-size:.72rem;font-weight:900}.drivehistmeta{font-size:.64rem;color:var(--muted);margin-top:3px}.drivehistline{display:grid;grid-template-columns:120px 1fr 1fr;gap:6px;margin-top:7px;align-items:center}.drivehistline b{font-size:.72rem}.drivehistnum{font-size:.76rem;font-weight:900}.drivehistwinner{margin-top:7px;font-size:.66rem;color:#cbd5e1}.drivehiststatus{font-size:.68rem;color:var(--muted);margin-top:6px}@media(max-width:650px){.drivehistline{grid-template-columns:1fr 1fr}.drivehistline b{grid-column:1/-1}.drivehistconnect input{min-width:0;width:100%}}`;document.head.appendChild(st);const sec=document.createElement('section');sec.id='driveHistoryPanel';sec.className='drivehist';sec.innerHTML=`<div class="drivehistbar"><div><h2 style="margin:0">Histórico comparado · operaciones de Drive</h2><div class="sub">Cada operación registrada se recalcula con las mismas fechas y el mismo capital en ambas AFP usando SBS real.</div></div><div class="drivehistconnect"><input id="driveHistKey" type="password" autocomplete="off" placeholder="Clave Drive (solo si falta)"><button id="driveHistRefresh" type="button">Actualizar</button></div></div><div class="drivehiststatus" id="driveHistStatus">Buscando la conexión de Drive guardada en este navegador…</div><div class="drivehistlist" id="driveHistList"></div>`;pAfter($('operationPanel'),sec);$('driveHistKey').value=localStorage.getItem(DRIVE_KEY)||'';$('driveHistRefresh').onclick=()=>{const k=$('driveHistKey').value.trim();if(k)localStorage.setItem(DRIVE_KEY,k);loadDriveHistory()}}
function pAfter(ref,node){if(ref?.parentNode)ref.parentNode.insertBefore(node,ref.nextSibling)}
function renderDriveHistory(items){const box=$('driveHistList');if(!box)return;const sorted=items.slice().sort((a,b)=>String(b.row.created_at||b.row.entry_date||'').localeCompare(String(a.row.created_at||a.row.entry_date||''))).slice(0,12);if(!sorted.length){box.innerHTML='<div class="sub">No hay operaciones en Drive.</div>';return}box.innerHTML=sorted.map(({fund,row})=>{const r=officialCompare(row.entry_date,row.exit_date,row.capital,!row.exit_date);const state=row.exit_date?'CERRADA':'ABIERTA',model=String(row.valuation_model||'qqq')==='new_tickers'?'B':'A',meta=`Registrada en ${fund==='PROFUTURO'?'Profuturo':'Hábitat'} · ${state} · Modelo ${model} · ${fmt(row.entry_date)} → ${row.exit_date?fmt(row.exit_date):'abierta'} · ${money(row.capital)}`;if(!r)return`<div class="drivehistcard"><div class="drivehisthead">${meta}</div><div class="drivehistwinner">Todavía no hay SBS comunes suficientes para recalcular esta operación.</div></div>`;const winner=r.pf>r.hf?'Profuturo':r.hf>r.pf?'Hábitat':'Empate',adv=Math.abs(r.pf-r.hf);return`<div class="drivehistcard"><div class="drivehisthead">${fmt(r.entry.fecha)} → ${fmt(r.exit.fecha)} · ${money(r.cap)}</div><div class="drivehistmeta">${meta}</div><div class="drivehistline"><b>Profuturo Fondo 3</b><span class="drivehistnum ${r.pg>=0?'pos':'neg'}">${signedMoney(r.pg)}</span><span class="drivehistnum ${r.pr>=0?'pos':'neg'}">${pct(r.pr)}</span></div><div class="drivehistline"><b>Hábitat Fondo 3</b><span class="drivehistnum ${r.hg>=0?'pos':'neg'}">${signedMoney(r.hg)}</span><span class="drivehistnum ${r.hr>=0?'pos':'neg'}">${pct(r.hr)}</span></div><div class="drivehistwinner">Más rentable: <b>${winner}</b>${winner==='Empate'?'':` · ventaja ${money(adv)}`} · cálculo SBS real, independiente del VC guardado antiguo.</div></div>`}).join('')}
async function loadDriveHistory(){const key=(localStorage.getItem(DRIVE_KEY)||$('driveHistKey')?.value||'').trim();if(!key){$('driveHistStatus').textContent='Falta la clave. Si ya conectaste Drive en Profuturo o Hábitat desde este navegador, pulsa Actualizar; si no, ingrésala una vez.';return}if($('driveHistKey'))$('driveHistKey').value=key;$('driveHistStatus').textContent='Leyendo operaciones de las hojas Profuturo y Hábitat…';const pu=(localStorage.getItem(PRO_URL_KEY)||PRO_DEFAULT).trim(),hu=(localStorage.getItem(HAB_URL_KEY)||HAB_DEFAULT).trim();try{const[p,h]=await Promise.all([jsonp(pu,'PROFUTURO',key),jsonp(hu,'HABITAT',key)]);const pr=(Array.isArray(p.rows)?p.rows:[]).filter(r=>String(r.fund||'PROFUTURO').toUpperCase()==='PROFUTURO'),hr=(Array.isArray(h.rows)?h.rows:[]).filter(r=>String(r.fund||'HABITAT').toUpperCase()==='HABITAT');renderDriveHistory([...pr.map(row=>({fund:'PROFUTURO',row})),...hr.map(row=>({fund:'HABITAT',row}))]);$('driveHistStatus').textContent=`Drive conectado · ${pr.length} operaciones Profuturo · ${hr.length} operaciones Hábitat · se muestran las 12 más recientes.`}catch(e){$('driveHistStatus').textContent='No se pudo leer Drive: '+String(e.message||e)}}
async function boot(){try{const q='?c='+Date.now();const[ps,hs,pd,hd]=await Promise.all([fetch('../data/series.json'+q,{cache:'no-store'}).then(r=>r.json()),fetch('../habitat/data/series.json'+q,{cache:'no-store'}).then(r=>r.json()),fetch('../data/dual_rolling30_monitor.json'+q,{cache:'no-store'}).then(r=>r.json()),fetch('../habitat/data/dual_rolling30_monitor.json'+q,{cache:'no-store'}).then(r=>r.json())]);const pOfficial=officialHybrid(ps,pd),hOfficial=officialHybrid(hs,hd);DATA={sbs:pair(pOfficial,hOfficial),sets:{sbs:pair(pOfficial,hOfficial),a:pair(modelMap(pd,'qqq'),modelMap(hd,'qqq')),b:pair(modelMap(pd,'new_tickers'),modelMap(hd,'new_tickers'))}};installMainControls();installOperation();installDriveHistory();if(localStorage.getItem(DRIVE_KEY))loadDriveHistory()}catch(e){const err=$('error');if(err){err.style.display='block';err.textContent='No se pudieron cargar los datos consolidados: '+String(e.message||e)}}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();