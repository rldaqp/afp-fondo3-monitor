name: ARCHIVO · despliegue 60/30 anterior

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Workflow retirado. No publica el antiguo 60/30/alt runtime sobre el visor dual Rolling 30."
