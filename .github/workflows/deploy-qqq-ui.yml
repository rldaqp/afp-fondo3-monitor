name: ARCHIVO · despliegue QQQ incremental anterior

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Workflow retirado. El visor vigente compara únicamente los dos modelos Rolling 30."
