name: Probar polinomial Modelo A Rolling 30 OOS
on:
  workflow_dispatch:
  push:
    branches: [migracion-github-actions]
    paths:
      - scripts/test_poly_model_a_rolling30_oos.py
      - .github/workflows/test-poly-model-a-rolling30-oos.yml
permissions:
  contents: write
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: migracion-github-actions
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy
      - name: Ejecutar
        run: python scripts/test_poly_model_a_rolling30_oos.py
      - name: Guardar
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add analysis/test_poly_model_a_rolling30_oos.json
          if ! git diff --cached --quiet; then
            git commit -m "Guardar polinomial Modelo A Rolling 30 OOS"
            git push origin HEAD:migracion-github-actions
          fi