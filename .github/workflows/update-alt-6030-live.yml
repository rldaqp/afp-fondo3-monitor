name: 60/30 nuevos tickers retirado

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Estado
        run: |
          echo "Workflow retirado: el antiguo modelo 60/30 ya no forma parte del visor."
          echo "El seguimiento automático queda concentrado en los dos modelos Rolling 30."
