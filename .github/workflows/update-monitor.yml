name: Rolling 90 retirado

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Estado
        run: |
          echo "Workflow retirado: el visor Profuturo usa únicamente los dos modelos OLS Rolling 30."
          echo "No se ejecutan más tareas programadas del antiguo Rolling 90."
